@model IEnumerable<LawyerAppointmentSystem.Models.Appointment>

@{
    ViewData["Title"] = "Appointments";
}

<div class="container mt-4">

    <div class="d-flex justify-content-between align-items-center mb-3">
        <h3>📅 Appointments</h3>
        <a asp-action="CreateOrEdit" class="btn btn-primary">
            + New Appointment
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">

            @if (Model == null || !Model.Any())
            {
                <div class="text-center py-5 text-muted">
                    <h5>No appointments found</h5>
                    <p>Create a new appointment to get started.</p>
                </div>
            }
            else
            {
                <table class="table table-bordered table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>#</th>
                            <th>User</th>
                            <th>Lawyer</th>
                            <th>Date</th>
                            <th>Problem</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int count = 1;
                        }

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@count</td>

                                <!-- User -->
                                <td>
                                    @(item.User != null ? item.User.FullName : "N/A")
                                </td>

                                <!-- Lawyer -->
                                <td>
                                    @(item.Lawyer != null ? item.Lawyer.FullName : "N/A")
                                </td>

                                <!-- Formatted Date -->
                                <td>
                                    @item.AppointmentDate.ToString("dd MMM yyyy, hh:mm tt")
                                </td>

                                <!-- Short Problem -->
                                <td>
                                    @(string.IsNullOrEmpty(item.ProblemDetails)
                                        ? "N/A"
                                        : (item.ProblemDetails.Length > 35
                                            ? item.ProblemDetails.Substring(0, 35) + "..."
                                            : item.ProblemDetails))
                                </td>

                                <!-- Actions -->
                                <td class="text-end">
                                    <a asp-action="Details"
                                       asp-route-id="@item.Id"
                                       class="btn btn-sm btn-info">
                                        Details
                                    </a>

                                    <a asp-action="CreateOrEdit"
                                       as
